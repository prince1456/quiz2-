<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <meta name="description" content="">
        <meta name="author" content="">
        <title>Starter Template for Bootstrap</title>

        <!-- HTML5 shim and Respond.js IE8 support of HTML5 elements and media queries -->
        <!--[if lt IE 9]> <%= javascript_include_tag "https://oss.maxcdn.com/html5shiv/3.7.2/html5shiv.min.js", "https://oss.maxcdn.com/respond/1.4.2/respond.min.js" %> <![endif]-->

        <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
        <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
        <%= csrf_meta_tags %>
    </head>
    <body>

        <div class="container">
            <div class="masthead">

                <div class="masthead">
                    <img class="img-responsive" src="http://www.air-it.co.uk/wp-content/uploads/2015/02/Service-Icons-04-320x320.png" alt="">
                    <div>

                        <div style="float: right">
                            <%= form_tag shows_path, :method => 'get' do %>
                            <p>
                                <%= text_field_tag :keyword, params[:keyword] %>
                                <%= submit_tag "Search", :name => nil %>
                            </p>
                            <% end %>
                        </div>

                        <h5>supporter</h5>
                        <nav>
                            <ul class="nav nav-justified">
                                <li class="active">
                                    <%= link_to "Home", supports_path %>
                                </li>
                                <li><%= link_to "Request List", shows_path %>
                                </li>

                            </ul>
                        </nav>
                    </div>
                </div>
                <hr>
                <div class="container">
                    <% flash.each do |name, msg| %>
                    <%= content_tag :div, class: "alert alert-#{ name == :error ? "danger" : "success" } alert-dismissable", role: "alert" do %>
                    <button type="button" class="close" data-dismiss="alert">
                        <span aria-hidden="true">&times;</span>
                        <span class="sr-only">Close</span>
                    </button>
                    <%= msg %>
                    <% end %>
                    <% end %>
                    <%= yield %>
                </div>
            </div>
        </body>
    </html>
